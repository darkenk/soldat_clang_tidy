#include <cstdint>

extern std::uint8_t mysprite;

int main_test()
{
    // replace with SpriteSystem::Get().IsPlayerSpriteValid()
    if (mysprite > 0)
    {
    }
    // replace with !SpriteSystem::Get().IsPlayerSpriteValid()
    if (mysprite < 1)
    {
    }
    // replace with !SpriteSystem::Get().IsPlayerSpriteValid()
    if (mysprite == 0)
    {
    }

    std::uint8_t mysprite;
    if (mysprite > 0)
    {
    }
    if (mysprite < 1)
    {
    }
    if (mysprite == 0)
    {
    }

    return 0;
}